{
  "name": "freda-ops-cockpit-server",
  "version": "0.2.13-dashboard-sync",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && find src -name '*.js' -maxdepth 4 -print0 | xargs -0 -n1 node --check"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "adm-zip": "^0.5.16",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "turndown": "^7.2.0"
  },
  "optionalDependencies": {
    "playwright": "^1.49.1"
  },
  "engines": {
    "node": "20.x"
  },
  "description": "Freda Ops Cockpit Beta 0.2.13 dashboard-first POS sync with Uber/Square diagnostics and operations modules"
}
